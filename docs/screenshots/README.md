# Clarity screenshots

These unchanged screenshots capture the real Svelte renderer with deterministic
sample queue data and placeholder thumbnails. They are documentation images,
not a claim that mocked downloads ran in the native app.

Source: the accepted preview renderer run
`clarity-20260919T015049Z-546e221a3ee7411d968f0114b2d6e3da` at 1340 by 850 CSS
pixels and 100% browser scaling. The UI is unchanged by the 0.7.9 version bump.
The original raw evidence remains in the ignored local `target/renderer-checks`
directory. The version number is not shown in these images.